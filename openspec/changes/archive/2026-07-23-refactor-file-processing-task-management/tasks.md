## 1. 重构基线与迁移准备

- [x] 1.1 记录现有 upload、document、rag、task 公共出口、routes、数据表和管理端调用清单，建立迁移对照表
- [x] 1.2 为旧 `/document/*` 与 `/rag/dataset-document/*` 管理端流程增加兼容边界说明，明确切换和移除条件
- [x] 1.3 确认“上传后进入 RAG”首期默认配置位置并补充服务端配置类型、默认值和启动校验
- [x] 1.4 确认历史 `document_processing_jobs` 数据量并选择一次性迁移或只读兼容方案

## 2. 统一任务数据模型

- [x] 2.1 扩展通用 `tasks` 表的租户、分类、业务对象、当前阶段、进度、数量和错误摘要字段
- [x] 2.2 新增 `file_processing_tasks` 扩展表，保存文件、文档版本、任务类型、执行序号、触发来源和配置版本
- [x] 2.3 新增 `file_processing_task_stage_runs` 表，保存阶段尝试、checkpoint、进度、耗时和错误
- [x] 2.4 增加同一 DocumentVersion 和配置仅允许一个活动内容任务的事务级互斥校验
- [x] 2.5 增加同一文件执行序号唯一约束和并发安全的序号分配逻辑
- [x] 2.6 更新数据库表注册、共享模型类型和表结构测试
- [x] 2.7 为历史任务迁移或兼容查询实现可重复执行的数据转换脚本与校验报告

## 3. Documents 功能目录与调用边界

- [x] 3.1 创建 `apps/server/src/hooks/documents` 功能目录和 README，不建立根公共入口
- [x] 3.2 将上传、验证、会话和策略能力归入 `hooks/documents/upload`，对象存储原语归入内部 `storage`
- [x] 3.3 将复杂文档查询、版本、删除和清理能力归入 `hooks/documents/document`
- [x] 3.4 将页面查询、预览任务、转换器和 runner 归入 `hooks/documents/preview`
- [x] 3.5 将解析器、标准化、Segment 与内容 runner 归入 `hooks/documents/document/content`
- [x] 3.6 将知识库文档关系与版本发布归入 `hooks/documents/rag`，知识库基础 CRUD 直接归 route
- [x] 3.7 删除旧 `hooks/file`、`hooks/upload`、`hooks/document`、`hooks/rag` 兼容出口和根 barrel
- [x] 3.8 更新中文 TSDoc、README 和边界审计，确认调用方精确导入且 routes 不访问内部原语

## 4. 文件处理任务定义

- [x] 4.1 将领取、lease、阶段记录和 worker 公共运行时归入 `hooks/documents/tasks`，任务业务 runner 留在 preview 或 document/content 功能目录
- [x] 4.2 实现文件处理任务创建，写入通用任务主记录和文件任务扩展记录
- [x] 4.3 实现活动任务幂等查询，重复请求返回已有任务而不是创建重复记录
- [x] 4.4 实现文件任务持久化领取、状态条件更新和并发 worker 限制
- [x] 4.5 实现任务心跳、超时检测和服务重启后的等待任务恢复
- [x] 4.6 实现执行序号、累计执行次数和最后成功任务查询
- [x] 4.7 实现取消状态检查和阶段边界安全中止
- [x] 4.8 实现失败重试与成功后再次执行，确保新建任务且保留历史记录
- [x] 4.9 实现任务结果摘要、稳定错误码、用户错误信息和脱敏技术日志

## 5. 版本内容处理与关系发布阶段

- [x] 5.1 实现读取阶段并复用已验证文件的可重复读取流
- [x] 5.2 实现解析阶段并迁移现有解析器注册、超时和错误分类
- [x] 5.3 实现标准化阶段并保留版本、统计与 checkpoint
- [x] 5.4 实现 Segment 阶段并保持确定性 ID、幂等替换和版本化配置
- [x] 5.5 实现内容发布阶段，批量条件发布仍以当前版本为 pending 的知识库关系并记录 Segment 数量
- [x] 5.6 在任务详情和共享类型中使用“内容处理”准确表达当前能力，不宣称已完成 Embedding 或向量索引
- [x] 5.7 实现阶段失败后的任务、文件和文档版本状态一致性更新
- [x] 5.8 实现阶段 checkpoint 恢复测试，验证重试不会重复创建解析块、Segment 或知识库关联

## 6. 上传处理意图与自动任务

- [x] 6.1 扩展文件管理上传初始化类型，支持 `enterRag`、`datasetId` 和处理配置版本
- [x] 6.2 在服务端校验 RAG 意图、知识库状态和调用者权限
- [x] 6.3 将处理意图与上传会话持久化，并保持通用附件上传可不携带该意图
- [x] 6.4 在文件验证成功后幂等创建文件处理任务
- [x] 6.5 保证重复完成请求、浏览器离线和完成响应重试不会创建重复任务
- [x] 6.6 实现未进入 RAG 文件的手动任务创建用例
- [x] 6.7 增加多文件上传后按文件独立创建并并行执行任务的集成测试

## 7. 文件聚合查询与 routes

- [x] 7.1 扩展文件列表查询，聚合当前任务、执行次数、最后成功任务和知识库摘要
- [x] 7.2 为文件聚合查询增加文件名、文件状态、处理状态、知识库和时间筛选
- [x] 7.3 新增文件处理任务创建、详情、取消和重试 routes；普通状态操作直接 ORM，需要同步派生状态的取消及复杂创建、重试调用业务函数
- [x] 7.4 上传和文件 routes 对普通查询直接使用 ORM，复杂流程精确导入业务函数，且不得访问存储或任务内部原语
- [x] 7.5 为旧 document 与 dataset-document routes 增加兼容转发和弃用日志
- [x] 7.6 更新 `@repo/types` 的文件列表、上传意图、文件任务、阶段和操作接口类型
- [x] 7.7 增加 routes schema、权限和错误响应测试

## 8. 统一任务中心服务端能力

- [x] 8.1 扩展任务列表服务，支持任务分类、业务对象、文件、阶段、触发来源和发起人筛选
- [x] 8.2 扩展任务状态统计并确保统计与列表使用完全相同的数据权限和筛选条件
- [x] 8.3 实现文件任务详情聚合，返回文件、执行序号、阶段时间线、结果和错误
- [x] 8.4 将取消和重试操作路由到对应文件任务公共服务，同时保留原系统任务停止逻辑
- [x] 8.5 增加按文件访问权、知识库管理权、租户和用户过滤的任务数据范围
- [x] 8.6 拆分查看任务、取消任务、重试任务和查看技术日志权限，并更新默认管理员权限
- [x] 8.7 为普通用户、知识库管理员和系统管理员增加任务可见范围测试

## 9. 文件管理页面重构

- [x] 9.1 移除文件管理中的“通用文件”和“上传会话”页签，改为单一文件列表页面
- [x] 9.2 使用 `VSchemaForm mode="search"` 实现文件名、文件状态、处理状态、知识库和时间筛选
- [x] 9.3 重构上传弹窗，展示服务端默认的“上传后进入 RAG”选择和目标知识库字段
- [x] 9.4 保留上传队列内部的暂停、恢复、分片进度和失败重试，不再提供独立上传会话页面
- [x] 9.5 在文件列表展示当前任务、当前阶段、执行进度、累计执行次数、最后成功任务和知识库摘要
- [x] 9.6 新增手动开始处理弹窗，并在存在等价活动任务时引导查看现有任务
- [x] 9.7 实现查看任务、重新执行、预览、下载和删除操作
- [x] 9.8 实现从文件行跳转 `system.task` 并自动携带文件筛选条件
- [x] 9.9 移除独立 RAG 文档上传与处理入口，保留必要的知识库实体管理入口

## 10. 现有任务中心管理端增强

- [x] 10.1 在现有任务中心增加全部、文件处理和系统任务分类切换
- [x] 10.2 增加等待执行、执行中、执行成功、执行失败和已取消状态统计
- [x] 10.3 扩展搜索表单，支持文件、阶段、触发来源、发起人和时间筛选
- [x] 10.4 根据任务分类切换系统任务列和文件任务列，保留原系统任务功能
- [x] 10.5 新增文件任务详情组件，展示业务摘要、阶段时间线、错误建议和结果
- [x] 10.6 将技术日志改为管理员可见的折叠区域并保持日志脱敏
- [x] 10.7 增加文件任务取消、失败重试、再次执行和查看文件操作
- [x] 10.8 支持读取文件管理传入的筛选条件并允许用户清除
- [x] 10.9 根据权限隐藏手动脚本添加、定时任务、取消、重试和技术日志操作

## 11. 兼容迁移与清理

- [x] 11.1 迁移或兼容展示现有 `document_processing_jobs` 和阶段历史，验证任务数量与状态一致
- [x] 11.2 将现有文档与知识库关联映射到文件任务结果摘要，不重复创建关联
- [x] 11.3 增加功能开关，在新旧文件处理入口之间支持可回滚切换
- [x] 11.4 停止管理端调用旧 document 和 dataset-document 处理流程并观察弃用日志
- [x] 11.5 稳定后移除旧顶层 hook 实现和不再使用的页面组件、routes 与共享类型
- [x] 11.6 运行依赖边界检查，确认 routes 只直接 ORM 或精确导入稳定业务文件，不导入内部原语

## 12. 验证与文档

- [x] 12.1 为任务状态迁移、执行序号、活动任务互斥、取消和重试增加单元测试
- [x] 12.2 为上传意图、自动创建任务、浏览器离线和重复完成增加 PostgreSQL/MinIO 集成测试
- [x] 12.3 为多文件并行、单文件失败隔离、服务重启恢复和 checkpoint 增加集成测试
- [x] 12.4 为文件列表聚合、任务中心统计、筛选和权限范围增加接口测试
- [x] 12.5 运行管理端 lint、类型检查和生产构建
- [x] 12.6 运行服务端 lint、类型检查、单元测试和生产构建
- [x] 12.7 运行 `pnpm turbo lint`、相关工作区测试和 `git diff --check`
- [x] 12.8 更新文件域、文件处理任务和任务中心 README，说明目录职责、状态语义、扩展新任务方式和当前 RAG 能力边界
- [x] 12.9 使用 OpenSpec 校验命令验证 change 全部 artifacts 和场景格式
